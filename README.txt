git init

git config --global user.email "your@email.com"

git config --global user.name "yourName"

git status

git add --all

git commit -m "Commit Message"

git remote add origin https://github.com/your-name/repository-name

git push -u origin master